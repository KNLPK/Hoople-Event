{
    "token": "string;",
    "refreshToken": "string;",
    "expiresIn": "number;",
    "user": {
        "id": "string;",
        "name": "string;",
        "email": "string;",
        "role": "'participant' | 'organizer' | 'teams';"
    }
}
